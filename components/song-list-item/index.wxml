<wxs src="../../utils/format.wxs" module="format"></wxs>
<view class="item">
<view class="top">
  <image class="image" src="{{item.coverImgUrl}}" alt="" mode="widthFix"/>
  <view class="play-counter">{{format.forMatCount(item.playCount)}}</view>
</view>
<view class="bottom">
  {{item.name}}
</view>

</view>
  
